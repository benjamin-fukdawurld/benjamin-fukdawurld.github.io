import styled from "styled-components";

const HomeButtonLink = styled.a`
  flex-grow: 1;
`;

const HeaderTag = styled.header`
  display: flex;
  flex-flow: row wrap;
  justify-content: space-evenly;
  align-items: center;
`;

export { HomeButtonLink, HeaderTag };
